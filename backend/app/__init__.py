"""
Application package for the GymSaaS backend (v2 refactor).

During refactor, this package provides a structured layout (core, db, api, models, utils)
while still delegating most business logic to the existing legacy `backend.main` module.
"""

"""
Application package for the GymSaaS backend (v2 refactor).

During refactor, this package provides a structured layout (core, db, api, models, utils)
while still delegating most business logic to the existing legacy `backend.main` module.
"""

