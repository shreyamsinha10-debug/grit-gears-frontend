[pytest]
asyncio_mode = auto
# One event loop for whole session so Motor (created at app import) stays in same loop
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
