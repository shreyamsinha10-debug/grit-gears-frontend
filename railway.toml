# Railway: build installs deps, start runs the server.
# In Railway dashboard, set Root Directory to "backend" for this repo.
[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
